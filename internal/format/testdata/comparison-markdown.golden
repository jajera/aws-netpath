# comparison — DIFFERS at security_group, host_firewall

- failing path 10.0.1.10 -> 10.1.2.30: blocked at host_firewall, symptom timeout
- reference path 10.0.2.20 -> 10.1.2.30: permitted

## differences

| layer | verdict | evidence | detail |
| --- | --- | --- | --- |
| security_group | DIFFERS |  | both paths pass at security_group on different entries: 1 entry only on the failing path, 1 entry only on the reference path |
| security_group | DIFFERS | `failing only: sg-0111111111111111a` | egress rule allows tcp/443 to 10.1.0.0/16 |
| security_group | DIFFERS | `reference only: sg-0222222222222222b` | egress rule allows tcp/443 to 10.1.0.0/16 |
| host_firewall | DIFFERS |  | host_firewall blocks the failing path and permits the reference path (1 entry only on the failing path, 1 entry only on the reference path) |
| host_firewall | DIFFERS | `failing only: firewall-cmd --zone=public --list-rich-rules` | zone public allows 10.0.2.0/24; source 10.0.1.10 is not covered |
| host_firewall | DIFFERS | `reference only: firewall-cmd --zone=public --list-rich-rules` | zone public allows 10.0.2.0/24; source 10.0.2.20 is covered |

_the failing path is blocked at host_firewall while the reference path is permitted._

## matched layers

- route (MATCH)
- nacl (MATCH)
- firewall (MATCH)

_entries omitted: a comparison reports differences only._

## incomplete comparisons

_every layer either path reported was compared._
